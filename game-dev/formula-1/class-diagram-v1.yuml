// {type:class}
// {direction:topDown}
// {generate:true}

[Game | canvas:HTMLCanvasElement;ctx:CanvasRenderingContext2D; KeyboardListener:KeyboardListener; gameState:string; winner:string; Car1:Car; Car2:Car | rollDice();loop();draw(); writeTextToCanvas();random()]->[Car|name:string; distance:number; xPosition:number, yPosition:number;color:string| setDistance(distanceRaced:number);getDistance();getxPostition();getyPosition(); getName();draw(ctx:CanvasRenderingContext2D);loadImage(source: string)]
[Game]->[KeyboardListener]